# Hands-on-Lab-Extract-Transform-Load-ETL-
The project focuses on performing Extract, Transform, and Load (ETL) operations using Python. In this project, you will:  Extract data from different file types such as CSV, JSON, and XML. Transform the extracted data into the required format, for example, rounding price values to two decimal places. Load the transformed data into a target 
